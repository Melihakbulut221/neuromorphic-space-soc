# 00 — Index: what this is, what exists, and where to start

Entry point to the document corpus. It states what the project is, what
physically exists in this repository today as against what is planned,
how the twenty-odd documents relate to one another, and which of them to
read first depending on why you are reading.

Convention, as everywhere in this repository: **[fact]** = measured in
this environment or read out of a file in the working tree;
**[estimate]** = derived or judged; **[planned]** = an intention with a
date attached and no artifact behind it yet.

Every count below carries the command that produced it. They were
re-measured on 2026-08-26 against the working tree rather than copied
from another document, because several documents state totals that were
correct when they were written and have since been overtaken by later
work — see section 6.

---

## 1. What this project is

A fault-tolerant system-on-chip for on-board AI inference in
small-satellite missions, implemented on a 130 nm open PDK with an
open-source RTL-to-GDS flow, by a solo developer.

The architecture class follows the Frontgrade Gaisler GR801 as published
in its April 2026 product brief — a RISC-V management processor, an
event-driven neuromorphic inference engine, on-chip SRAM and a set of
spacecraft interfaces — retargeted from 28 nm FDSOI to 130 nm at a scale
an open PDK and one person can actually carry. It is a clean-room
retarget, not a clone: no Gaisler or BrainChip IP is used, and the
GR801 serves as the requirement set and the yardstick. The reference
analysis is `docs/00-reference-brief.md` and
`docs/01-reference-decomposition.md`; the positioning rules that bind
every external claim are `docs/05-market-positioning.md` section 4.

Three things distinguish it from a hobby tapeout, and each is a
falsifiable claim rather than a slogan:

- **Spec-first with frozen golden models.** The bit-exact integer models
  in `sw/golden/` are the specification; RTL is verified against them in
  lockstep, not against a testbench author's expectations. See
  `docs/10-npu-mvp-spec.md` and `docs/11-verification-harness.md`.
- **Formal proof where the property is provable.** Seven blocks carry
  SymbiYosys property sets. See `docs/09-formal-verification-plan.md`.
- **Fault tolerance that is measured, not asserted.** TMR, SECDED and
  scrubbing are demonstrated by a seeded upset campaign that reports an
  outcome distribution per structure, judged against the golden model.
  See `docs/16-fault-injection-campaign.md`.

## 2. What exists today, and what does not

### 2.1 Exists — artifacts in this working tree [fact]

| What | Where | Evidence |
|---|---|---|
| RTL: AER FIFO, LIF core, NPU register bank, SECDED encoder and decoder, TMR voter, scrub controller, pilot top level, Tiny Tapeout wrapper | `hw/rtl/` (**9 `.v` files, 5,366 lines**, plus the generated `npu_regs.vh` header for 10 files and 5,491 lines in total; re-counted 2026-08-29, was "10 files, 3,800 lines" on 2026-08-26) | `wc -l hw/rtl/*.v` |
| Bit-exact golden models: LIF core, network, SECDED, register-map generator | `sw/golden/` | `docs/10-npu-mvp-spec.md` |
| Python suite: **295 tests collected, 295 passed**, re-counted 2026-09-01 for `docs/43` (was 196 collected on 2026-08-29 and 185 at commit `fe89f0d`). Unlike the earlier re-counts this one is an execution as well as a collection | `sw/tests/` | `.venv/bin/python -m pytest --collect-only -q` and `.venv/bin/python -m pytest sw/tests` at the repository root |
| cocotb suites: **145 test functions across 8 modules**, re-counted 2026-08-29 (was 129); seven of them reachable as `make -C hw/tb TB=<block>` | `hw/tb/` | `grep -c '@cocotb.test' hw/tb/test_*.py` |
| Formal: **45 SymbiYosys tasks across 7 property sets** (`aer_fifo` 4, `lif_ctrl` 8, `lif_mem` 8, `npu_regbank` 5, `scrub` 9, `secded` 3, `tmr_voter` 8), re-measured 2026-08-29 | `formal/` | the `[tasks]` sections of `formal/*.sby`; `make -C formal -n everything \| grep -c "sby -f"` counts them, `make -C formal everything` runs them all |
| Register map, single-sourced from `regmap/regmap.yaml`, with a sync test that fails if spec, generated document and RTL header drift apart | `regmap/`, `docs/regmap-npu.md` | `sw/tests/test_regmap.py` |
| IHP SG13G2 sign-off of a single block (`aer_fifo`) | `hw/openlane/aer_fifo/runs/trial-03-signoff/` | `final/metrics.json`: 0 Magic DRC, 0 KLayout DRC, 0 LVS errors, 0 antenna nets and pins, 0 XOR differences, 0 power-grid violations |
| IHP SG13G2 sign-off of the integrated 4x2 pilot at 52.4 % utilization. **The 52.4 % is the `tt-harden` run of 2026-08-25 and is three hardening waves stale; the current utilization is in `docs/22-reharden-wave5.md` and is not restated here because a further run was in progress on 2026-08-29** | `tt/runs/tt-harden/` | `final/metrics.json`: 0 Magic DRC, 0 LVS errors, 0 antenna nets and pins, 0 unmapped instances, 0 max-slew and 0 max-cap violations on all three corners |
| SKY130A harden of the same RTL, as a portability control | `hw/openlane/pilot_sky130/runs/sky-02-signoff/` | `final/metrics.json`: 0 Magic DRC, 0 KLayout DRC, 0 LVS errors, 0 antenna nets and pins, 0 XOR differences. Max-slew violations are *not* zero on this PDK; `docs/18-cross-pdk-portability.md` records why |
| A Tiny Tapeout submission tree for TTIHP26b, generated rather than hand-maintained | `tt/`, `scripts/gen_tt_submission.py` | `sw/tests/test_tt_submission.py` |
| A measured fault-injection outcome distribution for `pilot_top` | `hw/tb/fi_campaign_results.json` | `docs/16-fault-injection-campaign.md` |
| 27 markdown source files: 25 under `docs/` including this one, plus `README.md` and `ROADMAP.md`, re-counted 2026-08-29 | `docs/`, `README.md`, `ROADMAP.md` | `ls docs/*.md \| wc -l`; `_site/manifest.json` after `python3 scripts/build_docs.py` |

Two of the run directories above are git-ignored build output. On a
fresh clone they are absent and the artifact half of
`sw/tests/test_flow_evidence.py` skips rather than fails; the commands
that regenerate them are in that file's docstring and in
`docs/12-sg13g2-flow-bringup.md`.

### 2.2 Does not exist [fact]

- **No silicon.** Nothing has been fabricated. The TTIHP26b shuttle
  closes 2026-09-21 and its silicon is expected 2027-06-25 per
  `ROADMAP.md` section 1; every radiation and reliability statement in
  this repository is therefore pre-silicon.
- **No radiation test data.** No TID, proton or heavy-ion result exists.
  The fault-injection campaign is a simulation of single-bit upsets, not
  a beam test, and `docs/16-fault-injection-campaign.md` says so in its
  own limitations section.
- **No management CPU.** The RV32 core is surveyed in
  `docs/03-cpu-and-ip-survey.md` and not selected, integrated or
  verified.
- **No spacecraft interfaces.** SpaceWire, CAN, QSPI, SPI, I2C, UART and
  GPIO are scoped and costed; none is implemented.
- **No SRAM macro in any hardened design.** Every result above is
  flip-flop RAM. The RM_IHPSG13 macro study is in
  `docs/12-sg13g2-flow-bringup.md`.
- **No licence.** `docs/14-licensing-decision.md` is a decision memo with
  an unsigned decision section. Until it is signed the repository is
  unpublished and unlicensed, and that is why the documentation workflow
  in `.github/workflows/docs.yml` builds the site but deliberately does
  not publish it.
- **No funding.** `docs/13-nlnet-application.md` is a submission-ready
  draft. The NLnet Restack deadline is 2026-11-03.

### 2.3 Planned, with dates [planned]

The authoritative plan is `ROADMAP.md`; it carries the phase gates and
the external clocks. The near-term shape is: freeze and submit the pilot
before 2026-09-21, submit the NLnet application before 2026-11-03, build
the SoC through 2027, bring up pilot silicon from mid-2027. Effort
figures in `ROADMAP.md` phase P3 are tagged `[estimate]` and should be
read as such.

## 3. How the documents relate

The corpus is not a single narrative. It is five overlapping tracks laid
down in roughly chronological order, plus two review records that cut
across all of them.

**Research (00-06)** established what is being built and why. `docs/00`
summarises the GR801 brief; `docs/01` decomposes it and does the 28 nm
to 130 nm scaling; `docs/02`, `docs/03` and `docs/04` choose the
inference engine, the CPU and interface IP, and the technology and flow
respectively; `docs/05` fixes the positioning rules; `docs/06` covers
funding and the shuttle. Later measurement has superseded specific
figures in several of these, and where it has, the later document says
so explicitly — `docs/10` supersedes the `docs/01` physical envelope for
the NPU, and `docs/15` supersedes the `docs/06` tile arithmetic.

**Specification and verification (08-11)** turned the research into
something buildable. `docs/08` extracts the programmer-visible
conventions to stay close to; `docs/09` is the verification programme;
`docs/10` is the NPU specification the golden model implements;
`docs/11` is the operating manual for the whole harness and is the
document to read before running anything.

**Physical (12, 15, 18, 21)** is the evidence chain. `docs/12` brings up
the SG13G2 flow and hardens one block; `docs/15` states the pilot's die
content, pin contract and tile budget; `docs/18` hardens the same RTL on
SKY130A to separate design properties from PDK properties; `docs/21` is
the pre-silicon device datasheet that collects the pilot's
externally-visible behaviour in one place.

**Fault tolerance (16)** is the measurement that the hardening
mechanisms in the RTL actually cover the design, rather than merely
working when aimed at.

**Funding and licensing (13, 14)** are the two documents with an
external deadline and an unmade decision in them.

**Process (19)** compares this repository's CI against `gonsolo/borg`
and is where the case for this documentation site was made.

**Reviews (07, 17)** are the cross-cutting audits. `docs/07` closes the
research phase and design wave 1; `docs/17` closes design wave 2. Both
carry numbered findings with dispositions and both feed `ROADMAP.md`.
They are the fastest way to find out what is wrong with everything else.

## 4. Where to start

### 4.1 If you are evaluating the engineering

Read the two review records first — `docs/07-design-review.md` and
`docs/17-wave2-review-record.md`. They are adversarial by construction,
they cite line numbers, and they will tell you the weaknesses faster
than the documents that contain them will. Then:

1. `docs/10-npu-mvp-spec.md` — the specification, and the only place the
   update equations are normative.
2. `docs/11-verification-harness.md` — what is verified, how, and what
   each suite is entitled to claim.
3. `docs/09-formal-verification-plan.md` part C — the formal targets and
   which of them are green.
4. `docs/16-fault-injection-campaign.md` — the measured upset response,
   including the structures where the answer is bad.
5. `docs/12-sg13g2-flow-bringup.md` and
   `docs/18-cross-pdk-portability.md` — the physical results and the
   cross-PDK control.

The single most informative artifact is not a document: it is
`sw/tests/test_flow_evidence.py`, which re-reads the sign-off numbers
claimed in `docs/12` out of the run directory every time the suite runs,
so the gate is auditable rather than asserted.

### 4.2 If you are reproducing the results

Start at `docs/11-verification-harness.md`. It is the only document
written as instructions. In order:

1. Python golden-model suite — `.venv/bin/python -m pytest` from the
   repository root. 185 tests collect; at commit `fe89f0d` they ran 152
   passed, 1 skipped, and this index's link check adds 32 more
   [fact, 2026-08-26]. The one skip is an artifact test in
   `sw/tests/test_flow_evidence.py` that needs a run tree a fresh clone
   does not have.
2. cocotb suites — `make -C hw/tb TB=<block>` for `aer_fifo`, `regbank`,
   `lif`, `secded`, `tmr`, `pilot`, and `make -C hw/tb -f Makefile.scrub`
   for the scrub controller. These need `hw/.venv`, which is deliberately
   separate from the root virtualenv; `docs/11` section 3.2 explains why.
3. Formal — `make -C formal everything`. Tool discovery for the whole
   repository is in `tools.mk`.
4. Physical — `hw/openlane/run_trial.sh` and the per-target scripts under
   `hw/openlane/`; `docs/12-sg13g2-flow-bringup.md` pins the tool and PDK
   versions each result was produced with.

Everything is rootless. Nothing in this repository needs Docker,
`sudo`, or network access at run time.

### 4.3 If you are assessing this for funding

Read in this order:

1. `docs/13-nlnet-application.md` — the application package itself, with
   every form field drafted and every open decision collected in its
   section 7.
2. This document's section 2 — what exists against what is promised.
3. `docs/06-funding-and-shuttle.md` — the budget derivation and the
   shuttle logistics, noting that its tile arithmetic is superseded by
   `docs/15-pilot-tile-plan.md`.
4. `docs/14-licensing-decision.md` — the open-licensing decision, which
   is a precondition of NLnet funding and is **not yet made**.
5. `docs/05-market-positioning.md` section 4 — the rules that stop this
   project from overclaiming radiation tolerance, and against which any
   claim made to you should be checked.
6. `docs/07-design-review.md` and `docs/17-wave2-review-record.md` — the
   project's own account of its defects.

The two questions worth pressing are in section 2.2: there is no
silicon and no radiation data, and the licence is unsigned.

## 5. The documents

| Document | Purpose |
|---|---|
| `docs/00-index.md` | This page. |
| `docs/00-reference-brief.md` | GR801 public-brief summary and initial scaling observations. |
| `docs/01-reference-decomposition.md` | GR801 architecture decomposition and 28 nm to 130 nm scaling analysis. |
| `docs/02-npu-architecture.md` | Event-driven inference engine options, and the recommendation. |
| `docs/03-cpu-and-ip-survey.md` | Management CPU and interface IP survey: licensing and verification fit. |
| `docs/04-technology-and-flow.md` | 130 nm technology selection, memory strategy, flow and cost. |
| `docs/05-market-positioning.md` | Mission profile, competitive landscape, and the binding positioning rules. |
| `docs/06-funding-and-shuttle.md` | NLnet grant plan and TTIHP26b shuttle logistics. |
| `docs/07-design-review.md` | Independent review of the research phase and design wave 1. |
| `docs/08-gr801-datasheet-notes.md` | GR801/GRLIB programmer-visible conventions and a proximity checklist. |
| `docs/09-formal-verification-plan.md` | Formal verification programme: RTL formal, golden-model refinement, software track. |
| `docs/10-npu-mvp-spec.md` | NPU MVP micro-architecture specification v0.1 — normative. |
| `docs/11-verification-harness.md` | How to run and how to read every verification target in the repository. |
| `docs/12-sg13g2-flow-bringup.md` | IHP SG13G2 flow bring-up, `aer_fifo` trial harden, and the RM_IHPSG13 macro decision. |
| `docs/13-nlnet-application.md` | NLnet Restack application package, drafted field by field. |
| `docs/14-licensing-decision.md` | Open-licensing and publication decision memo — recommendation made, decision unsigned. |
| `docs/15-pilot-tile-plan.md` | Pilot die content, pin contract, tile budget and submission tree. |
| `docs/16-fault-injection-campaign.md` | Seeded upset campaign: measured outcome distribution and per-structure ranking. |
| `docs/17-wave2-review-record.md` | Independent review record for design wave 2, and the wave-3 plan. |
| `docs/18-cross-pdk-portability.md` | The same pilot RTL hardened on SKY130A, as a control on PDK-specific results. |
| `docs/19-ci-parity-borg.md` | CI parity assessment against `gonsolo/borg`'s six workflows. |
| `docs/20-reharden-and-corners.md` | Re-harden after the configuration-TMR fix, and why `PNR_CORNERS` cannot close the SKY130 slow corner. Supersedes area and timing figures in `docs/15` and `docs/18`. |
| `docs/21-pilot-datasheet.md` | Pre-silicon device datasheet for the submitted pilot. |
| `docs/22-reharden-wave5.md` | Re-harden after the wave-5 AER pointer TMR, and the tile budget it breaks. Supersedes area, utilization and timing figures in `docs/15`, `docs/18`, `docs/20` and `docs/21`; its section 6 is the location-by-location correction list. |
| `docs/23-tile-shape-decision.md` | Both twelve-tile shapes hardened and compared after `docs/22` put the design over the 4x2 budget. Decides **6x2**, on routing convergence, timing and clock skew rather than on area — both shapes clear the utilization criterion with room to spare. Records that 3x4 is not confirmed purchasable on TTIHP26b. |
| `docs/24-gate-level-simulation.md` | Gate-level simulation of the hardened netlist — the ROADMAP P1 bar item, and the direct check on the RTL-versus-netlist gap that has produced three separate defects in this project. Records which tests can run at gate level and which cannot. |
| `docs/25-sky130-6x2.md` | SKY130A at twelve tiles. Routes where 4x2 diverged, every manufacturability deck at zero on both PDKs, and the slow corner still missed — with a decomposition of the miss that supersedes the diagnosis in `docs/18`. |
| `docs/26-gate-level-fault-injection.md` | Fault injection on the hardened netlist rather than the RTL, with the validation that the method models a transient upset and not a stuck-at. 355 of 357 comparable injections agree with the RTL campaign; the two that do not are named and analysed. |
| `docs/27-reharden-wave6.md` | Re-harden after the timeout retirement and the three valid-flag rails. The netlist census guards pass by name rather than skipping, so the redundancy is confirmed present in the shipped netlist; records that the slow-corner margin has fallen to +0.32 ns — **corrected 2026-08-30: with the OCV derate `docs/28` found was never applied, that run is at -0.67 ns and does not close the slow corner**. |
| `docs/28-timing-recovery.md` | Where the slow-corner margin went and how it was recovered — and two flow defects found on the way: the OCV derate was silently zero on both PDKs, and the setup checker only ever examined the typical corner. Read this before quoting any timing figure produced before it. |
| `docs/29-queue-storage-protection.md` | One even-parity bit per queue entry, closing `evq_mem` — the last unprotected structure in the event path. Records why SECDED was deferred on codec area rather than on timing, and what parity still does not catch. |
| `docs/30-dispatcher-protection.md` | The dispatcher's five silent corruptions, and why they had to be detected rather than corrected: the replication bound proved for one bit generalises, and at two bits there are only six balanced coordinate functions, four of which the two rails already take. Two flip-flops for five records. |
| `docs/31-signoff-6x2.md` | Complete geometric and timing sign-off of the 6x2 pilot — and the finding that the submission path did not carry the settings that make it close, measured by hardening that path and watching it miss while every checker reported clean. |
| `docs/32-gate-level-refresh.md` | Gate level re-run against the current netlist: 370 of 370 comparable injections now classify identically with the RTL campaign, the new redundancy is exercised for the first time, and the one long-standing divergence is narrowed to the device boundary after its named hypothesis was tested and excluded. |
| `docs/33-rail-transform.md` | The dual-rail flags claimed two independent defences and the netlist carries one: `dfflibmap` folds the per-rail storage polarity away during technology mapping, because this library has no reset-to-1 flip-flop. The transform is measured to work where merging happens, proved unreachable at the netlist for a one-bit rail, and the three rail headers now say so. |
| `docs/34-pilot-freeze.md` | The freeze record: the exact artifact set that constitutes the TTIHP26b submission, pinned by hash — RTL blobs, netlist and GDS checksums, the `tt/` manifest, the six flow configs, tool and PDK versions — with the rule for what may change without re-running what. Closes two open items: max fanout is measured to be 83 clock-tree buffers plus one antenna artifact against a library limit no configuration key can reach, and the config generator now pins what it must not silently re-derive. |
| `docs/35-formal-progress.md` | Audit of the ten formal targets against the tree rather than against their own status column, two rows closed, and the mutation evidence — including a queue property set that a silently-lost write passed. |
| `docs/36-checker-closure.md` | The last two ungated timing checkers, closed. Max cap and max slew reported violations and failed on none, by a different mechanism than the setup defect before them: the step class supplies a match-nothing corner list that never falls back to the PDK's, so raising the general key would have been inert. All twenty-one checkers audited; a fourth instance found in `Checker.WireLength`; the re-harden this required reproduced the frozen artifacts bit for bit. Also states plainly that sky130 does not meet max slew — 3,930 violations that four corners' worth of runs had reported as clean. |
| `docs/37-external-check.md` | Re-verification of every external fact this project depends on, from primary sources. No deadline moved; the shuttle cost, the shuttle close time and the funder's own decision lag did. |
| `docs/38-ibex-bringup.md` | The CPU gate, and the first block of the SoC beyond the pilot: Ibex at a pinned commit through sv2v into Yosys, OpenSTA and Icarus on sg13g2, measured rather than estimated. sv2v carries the whole core with **zero patches to Ibex**; the translation is **not** proven equivalent and the document says so rather than implying it. Also records that sv2v is less of a single point of failure than `docs/03` prices — the pinned Yosys reads Ibex SystemVerilog directly through its bundled `slang` plugin, blocked by exactly one construct — that lockstep and register-file ECC are one inseparable switch costing +152 % area for detection rather than correction, and that the management core is under 2 % of the 25 mm2 envelope but does not fit a Tiny Tapeout block beside the frozen pilot. |
| `docs/39-soc-bus-and-memory-map.md` | The bus and the memory map. The map is frozen as `regmap/memmap.yaml` with a generator that emits the documentation, the RTL header, the C header, the linker `MEMORY` block and both device tables, and that refuses a map Ibex's fixed reset vector or 256-byte `mtvec` cannot reach. The interconnect is **not** AMBA AHB and none is claimed: `docs/08`'s own definition of GR801-proximity is the software-visible layer, every peripheral `docs/03` selected is native to a different bus, AHB-Lite has one master by definition where Ibex has two, and AHB folds grant and response onto one wire so arbitrating two masters costs 67 parking flip-flops. AMBA 3 APB *is* implemented, and proved compliant by k-induction, because it is small enough for the claim to be backed. The AHB path was built as an unverified cost probe and measured anyway — it is 0.85 % of the CPU more expensive, so area is explicitly not the reason. The docs/38 bring-up program runs unchanged out of the boot ROM through the real fabric with its console decoded off a serial line. |
| `docs/40-interrupts-timers-watchdog.md` | The interrupt and timing subsystem, and the watchdog. Three decisions argued rather than assumed. **No PLIC**: Ibex's fifteen fast local interrupts cover the thirteen sources the frozen map defines with two spare, the region stays reserved with a mechanical condition that reverses the decision, and the map now carries the plug-and-play source number and the wire index as two fields because they are two namespaces. **CLINT and GPTIMER both stay**: one owns monotonic time and the other reloadable intervals, and neither is a convenient way to do the other's job. **The watchdog escalates** -- NMI, then system reset, then an external pin -- cannot be disabled, slowed or acknowledged by software without a key, and keeps its state through the reset it causes, because `docs/38`'s decision to decline lockstep made it the core's only backstop. A timer interrupt is taken and returned from on the real fabric, the whole escalation ladder runs end to end across three boots of the SoC in one simulation, and the vectored-only `mtvec` of `docs/38` is exercised for the first time -- and bites. |
| `docs/41-watchdog-hardening.md` | The block whose job is to catch upsets, protected — and the ranking that decided what to leave alone. State is ranked by **persistence times silence**: what rewrites this register, and does its corruption announce itself. The eight fields nothing rewrites — the bootstrap latch, the stage-1 pending flag, the reset record and count, the reset stretch, the new mismatch report — are bundled into one 22-bit word and tripled, because six of them are single bits and three replicas cannot be held apart over one bit. `counter`, `reload` and `pre` are deliberately left as single points, and the price is measured rather than argued: an upset in them moves a deadline by at most the constant W2 already fixes, and never leaves the part disarmed. The voter is `hw/rtl`'s own, read in place and asserted to still be the blob the pilot freeze pins. **Guarded through synthesis and counted in the mapped netlist** — 102 flip-flops on two flows with every protective attribute deleted from the source text, dropping to 80 when the MIX transform is removed from both mixed replicas. Unlike `docs/33`, the transform reaches the mapped cells, and the document says why that is a bonus and not the defence. **+5,091 um2, +1.85 % of the Ibex core, 1.6 % of what `docs/38`'s declined lockstep would have cost**, and not one cycle of the whole-SoC run moved. 162 injections into the block's own flip-flops: 126 of 126 into the protected word CORRECTED, against a counterfactual run of the same campaign on the unprotected block where two single-bit upsets left the watchdog silently disarmed and one stopped it firing altogether. |
| `docs/42-core-fault-injection.md` | The management core measured, because `docs/38`'s decision to decline lockstep obliged it — and the first campaign in this repository that injects into one block and observes another's response. 1,300 stratified single-bit upsets into the Ibex `small-pmp` core running the whole SoC, **each run twice**: once with the watchdog armed and once with its bootstrap pin held high, because "the watchdog escalated" is not a catch rate until the same upset on the same machine with no backstop says whether the machine was in fact dead. A catch is a transition on a pin, never a timeout, and the budget leaves eleven complete escalation ladders in which to fire. **Design-weighted SDC 2.8 % ± 1.6; the watchdog catches 91.7 % [78.2 – 97.1] of the upsets that leave the core dead, with zero spurious escalations in 1,106 survivable injections and 27 failures turned into correct results.** Two findings decide the reading. **A corrupted loop bound keeps petting the watchdog** — all three uncaught dead runs are `x23`, which holds `FI_ROUNDS`, and the machine loops for ever while kicking on schedule; that argues for a windowed watchdog, not for a shadow core. And **45 % of the core's flip-flops are the register file, which carries most of the rate while the fifteen-bit controller carries the highest per-bit risk**, so the cheapest measured-value hardening is parity over 31 words rather than +112 % area for `SecureIbex`. The site list is verified complete against an independent Yosys census — 2,178 of 2,178 elaborated flip-flop bits — and section 9 says what a clean number here still would not license, starting with an oracle that sees the program's output and not the machine's architectural state. |
| `docs/43-core-hardening.md` | Both of `docs/42`'s recommendations built, and then measured — and one of them turns out to have been aimed at the wrong failure. **The architectural register file is SECDED-protected** with the codec this project already owns and has proved, corrected on read and scrubbed out of the storage, substituted into Ibex by a *file list* rather than by editing anything: the pinned checkout and its sv2v output are untouched, and section 3 prices the four things "zero patches" no longer covers — starting with a build whose Ibex is no longer bit-for-bit the one the commit names. The (72,64) codec shortened to 32 data bits lands exactly on (39,32), the minimal SECDED for a 32-bit word, because one row of H is empty over the low half; that is proved rather than noticed, and it is why the netlist holds 217 check flip-flops and not 248. The watchdog gains **W7**, a cadence contract, and **W8**, a per-phase kick budget. Section 4 answers the question a window forces: `WINS` is exactly a bound on the jitter ratio of the software's kick cadence, the `docs/42` workload measures **57.2** and can use no window at all, and one notch tighter than the shipped setting turns every kick of a healthy program into a violation. **The campaign is re-run with identical draws** — same site, same bit, same cycle, a byte-identical workload image — and the register-file strata go from 3 SDC and 4 HANG in 100 to **0 and 0 in 200**, design-weighted SDC **2.8 % ± 1.6 → 1.3 % ± 0.4**, and the watchdog's catch rate over the dead set from 33 of 36 to **28 of 28**. **And the honest negative:** the window caught **0 of 30** dead machines and produced **7 spurious escalations**, so `docs/41`'s zero does not survive it; the class `docs/42` recommended it for is caught by W8, 4 of 4 on the same records replayed verbatim, and removed at source by the register file. Costs, measured against baselines re-taken in the same session: **+38,050.58 um2 on the core (+13.8 %, 12.3 % of the declined lockstep)**, +4,904.10 um2 on the watchdog, **62.6 MHz down to 56.8 MHz** because the decoder sits in series on the register read path where lockstep's shadow core would have run in parallel, and 1,214 flip-flops counted in the mapped netlist on two mappers. 6 formal jobs, 25 tasks, 54 of 54 covers. Section 10 names what it leaves open, first among them that **nothing outside a simulator can see a corrected upset**: the substituted file carries upstream's port list and upstream's port list has no error output. |
| `docs/44-margin-and-observability.md` | The two things `docs/43` left: the timing margin its register-file codec spent, and the fact that nothing outside a simulator could see the codec work. **The margin turns out not to have been spent where `docs/43` said.** Every one of the ten worst setup paths at the slow corner, in *both* of that document's configurations, starts at `cheriot_enable_i[0]` — a configuration input `soc_top.v` drives with a constant — and a decoder fed by flip-flops cannot be on a path from a primary input. The 62.6 → 56.8 MHz is not a logic path either: at 17.62 ns the binding check in the hardened build is the **asynchronous reset recovery check on an unbuffered 2,328-fanout net**, and the two hardened builds report bit-identical slacks at every probe. Measured where it can be measured — the register file alone, where nothing else can move — the codec costs **2.7928 ns on the read path**; `FASTCORR` recovers **0.2593 ns of it for +1,201.55 um2**, taking the frozen decoder's 64-wide OR, eight-bit subtract and multiplexer off the data path, and is **proved bit-for-bit identical** to that decoder over a free codeword. The alternative that would have worked is priced and declined: hoisting the syndrome tree past the read multiplexer recovers **1.8677 ns, 66.9 %**, for **11.6 % of the core**, which is the wrong thing to spend on a margin that is not binding at a 20 ns target the design meets with 1.55 ns. The replay a stall would buy is rejected on a finding rather than a preference: Ibex has no input that stalls a mid-flight instruction and upstream's own `RegFileECC` does not replay either. **Observability is built**: `BUSSTAT` at `0xFF915000`, the slot the frozen map reserved in `docs/39`, with four saturating counters — corrected upsets, corrected read *cycles*, uncorrectable, and `docs/41`'s long-open TMRERR — the record in the power-on domain so a watchdog reset cannot erase what caused it and the interrupt enable in the system domain so the fresh boot is not interrupted by its own history. The fault line reaches the SoC through **three hunks in one generated file**, applied by a script whose every anchor must occur exactly once, and section 4 states the four things that costs — starting with a port-list guard weakened from equality to a prefix. All four of `docs/42`'s `x23` records replay with **software reading `CNT_RFSEC = 1`** out of a load instruction, and on one of them the bench counter says 2 and the SoC says 1, which is the difference between counting cycles and counting upsets, working. Costs: **+14.64 % core area as shipped**, of which the report is +5,172.55 um2; `soc_busstat` 7,060.55 um2. 22 checks, **185,443 cycles, not one moved**. 7 formal jobs, 29 tasks, 64 of 64 covers; 315 of 315 in `sw/tests`. **And `docs/43`'s campaign re-run in full comes back byte-identical** — the two 1,400-record files have the same md5 and the two logs differ only in the path they name, so the delta is zero at a resolution no aggregate could reach: design-weighted SDC 1.3 % ± 0.4, 28 of 28 dead machines caught, 0 spurious escalations in 1,228, 190 corrections all ending golden, 0 uncorrectable, and every stratum outside the register file unchanged down to the site table. The two zeros beside W7 and W8 in that report are the inert configuration behaving as `soc_wdog.sby` proves it must, not a measurement of either; `docs/46-watchdog-window-decision.md` is where W7's future is decided and nothing here contests it. Section 10's first entry is the sharpest thing here: **this flow's whole-core timing numbers have about 0.4 ns of noise**, measured on two controlled pairs, so every delta smaller than that in `docs/38`, `docs/43` and this document is inside it. |
| `docs/45-soc-top-synthesis.md` | The thing `docs/41` section 10 item 7, `docs/43` section 11 and `docs/44` section 10 each flagged and none did: **`soc_top` synthesised as ONE design**, so that the integrated subsystem is shown to synthesise at all and the per-block figures can be asked whether they compose. It elaborates at the first attempt with no edit to any RTL file, and it synthesises in **27 seconds and 250 MB** to **27,694 standard cells, 3,077 flip-flops, 392,932.8900 um2, 54.141 kGE** — which is **1.11 % SMALLER than the sum of the nine blocks re-measured the same afternoon**, so the areas compose to about one and a half per cent, an order this document then shows is the same as the mapper's own noise. The timing does not compose and the gap is measured rather than predicted: the core alone closes at 16.30 ns and **the SoC closes at 18.20 ns, 54.9 MHz** — **integration costs 1.90 ns and 6.4 MHz**, and section 7.4 says where it goes, because **the whole-design critical path leaves the core, crosses the fabric and comes back**, 12.43 ns inside `u_ibex` and 5.45 ns outside it, a path no per-block synthesis in this repository contains. **Its startpoint, in two independently mapped netlists, is the register file's read-address register** — the structure `docs/43` section 7.4 named and could not measure, and the first measurement in which `cheriot_enable_i` could not have been the answer; `docs/44` section 5.1's correction of the numbers under that sentence stands, and the sentence is now what the measurement shows. **The reset tree gets worse at the top, and structurally rather than by scale:** `rst_sys_n` reaches **2,766 reset pins** driven not by an input port with a `set_driving_cell` but by the weakest flip-flop in the library, for **58.9033 ns** of clock-to-Q, so the whole SoC as synthesised **closes nowhere below 80.62 ns, 12.4 MHz** — and unlike `docs/44`'s it poisons the SYNCHRONOUS group too, because `soc_bus.v` gates its request path with `rst_ni` for a reason its own header argues well. That is a place-and-route problem and the document says so without softening it. The worst hold path is `soc_top`'s own two-flop reset synchroniser. **The register-file protection costs +12.82 % at SoC scale against +14.64 % at core scale, and the 51 flip-flops of difference are exactly the three BUSSTAT counters that exist only because there is something to count** — telemetry a measurement of the core alone cannot see; in frequency it costs 16.6 MHz against 16.8 measured on the core, which is the one thing here that composes almost exactly. **And a finding about the instrument rather than the design:** `soc_fabric_meas` measures 16,379.99 um2 today against `docs/40`'s 16,229.24 and reproduces that older number **exactly** once the whole source list is restored — editing `soc_wdog.v` moved the measured area of a block that does not contain it, so **a per-block area under `flow/syn_soc.sh` is a function of the file list and not of the block**. Ten prior numbers reproduced first, two netlists byte-identical, three `slack.rpt` files byte-identical across a refactor. The memories are the one thing that cannot be synthesised — 589,824 registers of behavioural model, with `proc` alone on the RAM taking 28 minutes and 2.8 GB — and section 3 states both boundary models and the four things neither reproduces. Section 8's sharpest entry: three of the four timing results are about things that only exist before layout, and the number that decides the part, 54.9 MHz against a 50 MHz target, is 9.8 % of headroom on a netlist with no clock tree. |
| `docs/46-watchdog-window-decision.md` | `docs/43` section 12 item 5, answered: W7, the windowed watchdog, decided from a **second workload** rather than from the one that recommended it. The workload is what `docs/09` part B track 3 actually chose — a **static partitioned supervisor**, four partitions in U-mode isolated by PMP with the region reprogrammed on every dispatch, a periodic frame tick from CLINT, run-to-completion partitions returning by `ecall`, and unequal data-dependent work — run by `docs/42`'s instrument with **not one line of the testbench, the site table or the classifier changed**. **The kick placement was argued and written down before any cadence was measured**, in the source file, rejecting three placements and taking the one that ties the kick to the work rather than to the clock — which is the choice that measures *worse*. **The campaign — 812 stratified injections, 58 per stratum, each run twice — is the first in this repository in which the window catches anything:** a verbatim replay against a `WINS = 0` build shows it turned **two silent data corruptions into announced failures**, and caused **two of the four spurious escalations** (the other two are ordinary expiries at the tighter timeout a window obliges). The watchdog catches 32 of 32 dead machines, all by ordinary expiry; W8 fires zero times. **The recommendation is to remove W7 before floorplanning — not because it is worthless, but because its measured benefit, 0.25 % [0.07 – 0.89], and its measured cost, 0.27 % [0.08 – 1.00], are the same size from one site each, and because whether any window is feasible at all turns on a dispatch discipline `docs/09` has not chosen:** the same supervisor run work-triggered instead of time-triggered measures **2.104** against `WINS = 1`'s bound of 2.0, and then no timeout exists that satisfies both bounds. Five requirements that would have to enter `docs/09` track 3 to keep it, and three triggers that would reverse the decision, are stated. A finding that is not about the watchdog: the PMP registers are now the **worst stratum in the campaign by wrong answers**, 23 of 58, because the S2 isolation mechanism made them live architectural state that nothing had priced. Costs W7 makes visible that `docs/43` did not price: a window forces the timeout into a band `i_max < T < 2*i_min`, and it turns five properties of the supervisor into requirements on `docs/09` track 3. A control shows PMP is actually enforcing — a partition reading outside its region faults six times, `mcause = 0x5`, and the supervisor terminates it and carries on — because a clean run of this program would look identical with PMP switched off. |
| `docs/47-soc-place-and-route.md` | `docs/45` section 9 item 1 — "until something is placed, the number that decides the part is not a synthesis number" — done. **`soc_top` placed and routed whole, with 72 KiB of memory that is memory**, and the answer is negative and specific: **it does not close at 50 MHz, it closes at 43.10 MHz** — −3.2029 ns of setup at `nom_slow_1p08V_125C` on 2,003 endpoints, on extracted parasitics, with the derate actually applied — and it also fails hold at the fast corner (−0.2015 ns, 3 endpoints), max cap at all three and max slew at two. What it *does* do is route: **0 detailed-routing DRC errors, 0 disconnected pins, 0 power-grid violations, 1 antenna violating net**, 37,156 nets and 3,095,532 um of wire. **The reset tree turns out to be two problems and only one of them was place-and-route's.** `docs/45` found `rst_sys_n` driving 2,766 flip-flop reset pins **and two data pins** — `soc_bus.v`'s `can_issue` gates — which made the synchronous group (−60.6191) worse than the asynchronous one (−53.3681). Replacing that gate with a **locally registered `issue_en` — one flip-flop, one cycle of latency at boot, strictly more conservative behaviour** — moves the synchronous group to **+4.3006 on the identical memory model** and the closing period from **18.20 ns to 15.71 ns, 54.9 → 63.7 MHz**, on a controlled 2×2 matrix whose baseline row reproduces `docs/45` section 7.1 digit for digit. The other half needed nothing: **`repair_design` builds the 2,611-sink tree unasked and leaves +14.7319 ns of recovery margin**, so `docs/45`'s "a tree, not a bigger driver, and that is a floorplan input" was right about the tree and wrong about the difficulty. **The memories are six real RM_IHPSG13 macros** — four `1P_2048x64` for the 64 KiB RAM, two `1P_1024x32` for the 8 KiB ROM, **2,246,899.85 um2, 4.29 times the logic and 46.9 % of the die** — and the library was measured rather than quoted: across the eighteen single-port parts **the read arc is a function of DEPTH and spans 5.2678 to 9.6611 ns at the slow corner while density improves monotonically in the same direction**, so depth trades area against cycle time by 4.39 ns. The densest 32-bit part, `1P_8192x32_c4`, is excluded not on timing but because **it is one of only two parts in all 27 with no `A_BM` write mask**, and Ibex emits `sb` and `sh`. **Two findings about the flow that are not about this design.** `SYNTH_HIERARCHY_MODE: deferred_flatten`, copied from the pilot, **stops `soc_top.v`'s constant `cheriot_enable_i` at the module boundary and the CHERIoT logic survives** — 55 netlist lines against zero, 32 % more area, and **−9.9731 ns against +3.8711 ns of pre-layout synchronous slack, measured by one tool before any placement exists**; `flatten` instead leaves `soc_tmr_bank`'s three `keep_hierarchy` instances unmapped, so LibreLane can express neither half of what `flow/syn_soc_top.sh` already does and the flow hardens that script's netlist instead. And **`RUN_POST_GRT_DESIGN_REPAIR` and `RUN_POST_GRT_RESIZER_TIMING` default to `False`**: global-route parasitics cost this floorplan 6.72 ns and by default nothing is asked to recover any of it, which is worth **4.30 ns** and is announced by one line of log. **Every timing checker is bound to every corner before the run rather than audited after it** — the derate as a float, and all four `*_VIOLATION_CORNERS` as `["*"]` — and `checker_audit.py` says **17 of 19 in-flow checkers gate fully**, the flow exits non-zero naming each failing corner, and **`design__violations` reads 0 anyway**, which is `docs/23` section 3.2 recurring. **This is not a sign-off**: Magic DRC, KLayout DRC, XOR and LVS are off, because `docs/12` section 7.5 already measured them at 1,106,478 / 2,316 / 359 over ONE of these macros with every error inside it. Eight new guards, each mutation-tested. The honest caveat on the headline: the standard cells sit in a 1,851.8 × 790.0 um strip at 18.9 % density with a 2,332.02 um longest net, so **43.10 MHz is the frequency of this design in this floorplan, and no second floorplan exists yet.** |
| `docs/48-floorplan-and-capacitance.md` | `docs/47` section 8.4's closing sentence — **"43.10 MHz is the frequency of this design IN THIS FLOORPLAN, not of this design"** — was labelled a hypothesis with three pieces of evidence and no control. **This is the control, and the hypothesis does not survive it.** Two more floorplans were built by a generator that reads the macro dimensions out of the PDK's LEF, asserts site alignment, and **reproduces `docs/47`'s own floorplan byte for byte at `--channel 700.08 --density 40`** — one sized to the netlist actually hardened, which is the experiment `docs/47` section 11 item 3 specified, and one with the die **19.8 % narrower** and the two ROM macros moved out of the macro rows into the channel as islands, against that document's own complaint that "a strip is a bad shape for timing". **Both are worse, at every stage that was measured.** Post-CTS: **−10.7556** for `docs/47`'s floorplan against **−11.5970** and **−12.4536**. After global routing on real parasitics the narrow one is **−16.9653 against −6.8764**, laying down **5,433,782 um of global-route wire against 3,771,252 — 44.1 % more on 71 fewer nets**; the tight one's global router **did not terminate in 33 minutes against 8.033 seconds**, on two independent attempts. The mechanism is measured rather than argued: **`PL_TARGET_DENSITY_PCT` was 40 against a standard-cell utilization of 18.9 %, so the density penalty never bound and `docs/47`'s placement is already the HPWL-optimal one for these macro pin positions** — shrinking the box removes feasible solutions, and the cells fall from **27.0 % of their own bounding box to 14.4 % and 11.9 %**, with **7,857 of 27,558 exiled into the two ROM pockets** at the far end of the core from the RAM they talk to. **Two of the three leads this document was given turn out to be already spent**, and the run directories say so: `full2` hardens `syn_soc_top.sh`'s netlist with `Yosys.Synthesis` skipped, so `deferred_flatten`'s 13.84 ns was taken before 43.10 MHz existed — and the pilot's stated reason for that key **does** apply here, which is why a third option was needed rather than a switch — while `full3`, the run that produced −3.2029, already has both `RUN_POST_GRT_*` keys `True`. **Remaining value: 0 ns each.** Resolving the sign-off critical path against the netlist produces a **correction to `docs/47`**: that document's critical path is the **post-CTS** one and reproduces exactly (`raddr_b_i[1] → u_clint.mtime[9]`), but at **sign-off** the binding path starts at **`u_ram.g_ram_2048x64.u_b0/A_DOUT[15]`** and ends at a **register-file SECDED check bit**, with 444 of the 2,003 violating endpoints behind that macro. Of its 13.1261 ns of logic, **0.2481 ns is wire delay and 12.8780 ns is cell delay**: **the wires are not slow, they are heavy**, and six stages of 62 to 202 fF carry 7.3585 ns of it on cells — `a22oi`, `o21ai`, `xnor2`, `nand3`, `nand4` — that **`sg13g2_stdcell` ships in exactly one drive strength**. That is a tenth entry for `docs/41` section 6.6's list and the first in the PDK rather than the tool: **`MAX_CAPACITANCE_CONSTRAINT` is `Optional`, neither sky130 nor ihp-sg13g2 ships a value, and every signed-off SDC in this repository contains no `set_max_capacitance` at all** — so the only limit is the library's `default_max_capacitance : 0.3`, at which **one `sg13g2_o21ai_1` costs 3.395 ns at the slow corner, 17.0 % of a 20 ns period on one gate** [the vendor's own `cell_rise` table]. Tightening it to **0.0648 pF — the largest of the library's own seven characterisation loads at which every cell on the measured path stays under 1 ns** — was then run end to end **against a bit-identical placement** (global-placement HPWL agrees to all seven digits), and **it is worth +3.4722 ns before the timing resizer runs and −1.6525 ns at sign-off**: `repair_design` buffers **4,163 nets instead of 127**, the design ends with **13,745 timing-repair buffers against 7,910**, and `repair_timing` — the instrument that targets the critical path — manages **615 resizes and 260 buffers against 1,620 and 528**. **A global electrical constraint spent the budget the targeted repair needed.** It is not free in the other direction either: **it closes `docs/47`'s hold failure (−0.2015 ns on 3 fast-corner endpoints → +0.0494 on 0) and its max-slew violations, 14 at the slow corner and 1 at the fast**, and `checker_audit.py` on that run reports **17 of 19 in-flow checkers gating fully — the same as `docs/47`** — with `Checker.HoldViolations` and `Checker.MaxSlewViolations` confirmed bound to all three corners before reporting `all 0`, so the improvement came through the gates that were watching and not past them. **Nothing here closes and nothing beats 43.10 MHz**; the best sign-off number in this document is still `docs/47`'s. What is left is RTL or the target, and both are named: `docs/44`'s `SYNPRE` was declined by `docs/47` on the premise that the gap was floorplan, **that premise is now measured false**, and its 1.8677 ns is the right order against 3.2029 ns; and 50 MHz entered this project as the **Tiny Tapeout pilot's declared clock** while `docs/02` states the architecture target as a **range, 25–50 MHz**, which 43.10 is inside — but **no document here derives a throughput requirement from the clock**, so revising the target would be revising it against nothing, and the document declines to. Three guards, each mutation-tested. No RTL was modified. |
| `docs/49-synpre-and-the-binding-path.md` | `docs/48` section 11 item 1 promoted **`SYNPRE`** from declined to first, on the sentence that it "acts on the SECDED encoder that terminates the binding path". **It does not, and that is the whole result.** `SYNPRE` restructures the SECDED **decoder** on the register file's **read** port; the binding path runs `SRAM macro → load return → the WRITE encoder → a check-bit flip-flop`, one `secded_enc` already in front of the storage with nothing to hoist it past. **The two cones are disjoint**, so of the 2,003 violating endpoints the **444 behind the macros (worst −3.2029) are unreachable** and the **1,559 `SYNPRE` can reach have a worst of −2.0227** — **the most a perfect, free `SYNPRE` could do to the worst slack is nothing.** It was built and hardened anyway, because a pre-layout structural argument is what `docs/47` and `docs/48` exist to distrust. **It does not close: −3.0838 ns on 1,783 endpoints, 43.32 MHz, against `docs/47`'s −3.2029 on 2,003 and 43.10 MHz — a delta of +0.1191 ns, inside `docs/44`'s measured 0.4 ns of instrument noise and NOT reported as an improvement.** What it does buy is bulk: **setup TNS −2,075.11 → −1,446.11, −30.3 %**, and 220 fewer violating endpoints, for **+2,684 cells and +27,237.7728 um2 of synthesised standard cells, +6.9 %**. The lever is real where it acts — **at post-CTS it is the largest single-change improvement measured on this design, −10.7556 → −7.2915, +3.4641 ns**, on the same read-address-to-`mtime` structure in both netlists, with the arrival falling **31.8640 → 28.4130 ns** and **five `xnor2` cells on the path becoming two** — and **routing gives all of it back**: after global routing `syn1` is **−7.6924 against −6.8764**. The mechanism is measured: **`docs/48`'s density regime is unchanged** (utilization 18.9 % → 20.2 % against a 40 % target that still never binds) but **the cells spread rather than compact, 27.0 % of their own bounding box → 22.1 %**, global-placement HPWL **+23.2 %** and routed wirelength **+13.8 %**, and the electrical figures follow: **max slew at the slow corner 14 → 126 and max cap 10 → 24, both against the library's OWN 0.3 pF and 2.5074 ns in both runs, so directly comparable and unambiguously worse.** `syn2` has **0 hold violations at all three corners** against `docs/47`'s 3, through a gate `checker_audit.py` confirms examined three corners before reporting `all 0` — **but the movement is 0.2498 ns on three endpoints, inside the same noise, and this document does not attribute it to `SYNPRE`.** **17 of 19 in-flow checkers gate fully, the same as `docs/47` and `docs/48`**, and the flow exits non-zero on three criteria where `docs/47`'s exited on four. **Composition with `docs/48`'s capacitance constraint was declined and the premise measured false**: `SYNPRE` frees no setup margin on the binding path, its `RepairDesignPostGRT` behaves like the baseline's (94 / 109 / 128 nets against 101 / 110 / 127) so it makes no room in the step `docs/48` measured as starved, and it moves the cap and slew counts up rather than down. The change needed a proof `docs/44`'s C5 does not give — `SYNPRE` substitutes an **encoder's** output for the **decoder's** syndrome, and `hw/rtl/secded_enc.v` and `secded_dec.v` carry **separate copies of H that nothing had ever compared** — so **C6 and C7 were added and proved** on four tasks with **9 of 9 covers reached** and C6 mutation-tested to FAIL; the 124-injection campaign is identical field for field and the whole-SoC run is **cycle-identical, 22 checks / 174,128 / 185,443 / 587 / 0**. An eleventh entry for `docs/41` section 6.6's list, in the simulator this time and **repaired rather than only reported**: **`iverilog -P` on a hierarchical path elaborates, exits 0, warns about nothing and changes nothing**, and the first whole-SoC result was a run of the design without the change. **Nothing short of registering the RAM read data reaches 20 ns**: the macro arc alone is **9.5277 ns, 47.6 % of the period**, and splitting the path at the macro's output leaves **+9.9231 and +6.3247 ns** on the report's own decomposition. **50 MHz is not revised**, and the reason is sharper than `docs/48`'s: the target's provenance is a Tiny Tapeout tile with **no SRAM macro in it**, `docs/05`'s four mission profiles are all event-driven and **none is specified in cycles**, so the obligation is to size the workload rather than to move the number. One new guard, three mutation-tested assertions. No RTL was modified. |
| `docs/50-memory-read-register.md` | `docs/49` section 14 item 1 named this block: registering the RAM's read data is the only mechanism measured anywhere in `docs/47`, `docs/48` or `docs/49` that puts both halves of the binding path inside 20 ns, and *"what it costs is not silicon, it is a cycle"*. **Both halves of that sentence are right, and the cycle is what decides it.** The register was built as `RDREG` on `soc_mem`, placed AFTER the bank and half multiplexers so it is 32 flip-flops per memory rather than 256 and 64, and hardened on `docs/47`'s floorplan with a configuration whose `resolved.json` is **identical to `full3`'s key for key** — the only difference between the two sign-offs is which netlist went in. **It does exactly what it was built to do, completely: of `docs/47`'s 2,003 violating endpoints, 444 were launched by an SRAM macro's `A_DOUT` and now ZERO are.** The sign-off is **−2.4257 ns on 1,893 endpoints, 44.5917 MHz**, against **−3.2029 on 2,003 and 43.0981 MHz** — **+0.7772 ns, which is 1.94x `docs/44`'s measured 0.4 ns noise floor and is therefore the FIRST setup improvement in four documents that this repository is entitled to claim.** It still misses 20 ns by 1.6 ns, and **setup TNS gets 26.8 % worse** (−2,075.11 → −2,630.63) because the 444 endpoints the macro was hiding re-report under their read-address launch: **1,889 of the 1,893 now launch from ONE flip-flop, `register_file_i.raddr_b_i[1]`**, which is precisely the cone `docs/49` section 14 item 2 wrote a reversal condition for. Cheap everywhere the previous attempt was expensive — **+979 cells, +67 flip-flops (32 + 32 + 1 + 1 + 1, arithmetic that checks), +0.79 % area, +1.9 % routed wire**, and `ResizerTimingPostGRT` runs **23 min against the baseline's 22.5**, where `docs/49`'s ran 6 h 19. **AND THE PART GETS SLOWER, WHICH IS THE FINDING AND IT STOPS THE CHANGE.** The bring-up program takes **185,443 → 232,232 cycles, +25.23 %**, and `docs/46`'s supervisor — a different program, different startup file, different testbench — takes **29,176 → 36,680, +25.72 %**, the two agreeing to half a percent because this is a property of a two-stage core with no cache and not of a program. Attributed by registering one memory at a time: **RAM +26,820, ROM +20,118, additive to 0.3 %**, which is **0.9714 cycles per RAM response** (a load stalls the pipeline outright) against **0.1858 per ROM response** (the prefetch buffer hides 81 % of it). Multiplying cycles by period — the first time this repository has done so — **4.3028 ms → 5.2080 ms, 21.04 % SLOWER**, and **7.94 % slower even if it closed 20.000 ns exactly**: the break-even clock is **53.97 MHz** and the layout reaches 44.59. **The protocol turned out not to be the work.** `soc_bus.v` is byte-identical to `docs/47`'s: its rule 3 already allows a response "one or more cycles after the grant", its formal environment constrains slave response timing **not at all** so the k-induction proof already quantified over two cycles, and its one conservatism — refusing a grant in the cycle a response returns — was implemented, run, and measured at **zero cycles**, because **Ibex's `NUM_REQS` is 2 and equals the fabric's `MAX_OUT`**, so the core stops asking before the fabric stops granting. Measured by `hw/soc/tb/soc_bus_probe.v`, now in the tree: the instruction port sits at the limit for **34,689 cycles** and is **never** requesting in one of them, and the same-slave restriction's dead cycle is **never paid** in either configuration. **A red result nobody ran, which is `docs/41` section 6.6's shape with the sign reversed:** `hw/soc/formal/soc_bus.sby` has been **FAILING since `docs/47`** — that document added `issue_en`, priced its one dead cycle at boot in the RTL's own comment, and did not re-run F9, whose two-cycle fairness bound has no room for it. The counterexample is at depth 3. **F9 is extended, not relaxed**: F9a gains the precondition that the fabric is open and F9b bounds how long it may stay shut, which is strictly stronger than the version that was failing; every other formal suite passes at HEAD, checked. New verification: **a cocotb suite for `soc_mem`, which had never had one** — 7 tests at both parameter values, with the latency **measured from the DUT** rather than trusted, mutation-tested with one surviving mutation reported and explained; **a two-cycle-memory test in the fabric suite** whose vacuity guard was written by watching a wrong version of it fail; **four covers** witnessing the two-cycle regime, 16 of 16 reached; **a guard file**, 6 tests, four mutations. **17 of 19 in-flow checkers gate fully**, the same as `docs/47`, `docs/48` and `docs/49`, with `Checker.SetupViolations` and `Checker.HoldViolations` both confirmed bound to three corners — so the improved setup and the clean hold came through gates that were watching. The hold closure (−0.2015 on 3 → +0.1029 on 0) is **0.3044 ns and is NOT attributed**. One more thing the flow will not let go: at `RESP_CREDIT = 0` a term that folds to constant zero **changed the netlist by 272 cells**, while **52 lines of pure comment changed nothing** — the mapper is chaotic with respect to the EXPRESSION, not the file, and the control here is `docs/47`'s netlist **identical up to three net names**. **Recommendation: do not keep it**, with three stated reversal conditions; keep everything in section 8 regardless. About 2.1 h, against `docs/49`'s 9.0. |
| `docs/51-npu-integration.md` | `docs/39` section 9 item 4 has been one line in every document since: *"No NPU connection. The 256 MiB window is frozen and reaches the error slave."* **This is the connection, and the repository stops being a RISC-V SoC next to a separate accelerator.** `hw/rtl/pilot_top.v` — the TTIHP26b submission, frozen by blob hash in `docs/34`, **instantiated and not copied** — is inside `soc_top.v` and reached over the same four serial pins the die will have. **A program on Ibex, out of the boot ROM, over the real fabric, configures it, loads four ECC-checked weight words, feeds it a six-frame event stream and reads the spikes back — and the answer is checked against `sw/golden/lif_core.py`, computed at BUILD time into the ROM image, rather than against what the hardware produced.** 27 checks, fail mask 0, **213,971 cycles**, twelve output words matching event for event in stream order plus the whole neuron state file, and the hardware's own count of 22 events delivered and 12 drained agreeing with the stream the program collected by an independent path. **The connection style is argued rather than assumed, and the usual argument for it is attacked first.** "The die has only a serial port, so a parallel port could never be validated against real silicon" is weaker than it looks — the SoC is a different part on a different schedule, so a bench harness needs a serial host either way. What decides it is that **option (a) leaves ONE implementation of the die's host protocol and it is the one in the SoC's own regression**; a parallel port on a copy would need the read side effects re-timed (`EVQ_OUT`'s pop is tied to the command byte, which is the register `docs/50` section 5.1 spends four pages on) and would put the seventh instance of "something was checked and it was not the thing that ships" into a repository already bitten six times. **What was built is neither (a) nor (b) but a transport under an unchanged memory map:** software sees `SOC_NPU_BASE + node*0x1000 + offset` as ordinary loads and stores, and a future node with the native 32-bit configuration bus `docs/10` section 1 specifies is a change of transport under an unchanged driver. **The die's two ports are not symmetric and the asymmetry is in the pin contract, not in this work: SYNC has no input pin** (the pin word's TYPE is built from `AER_IN_TICK` alone, so it is 00 or 01 and never 10) **and there is no TYPE on the output pins at all** — a SYNC echo and a spike from neuron 0 are the same four bits — so the frame barrier that `docs/10` section 7.1 makes the determinism primitive travels the serial port in both directions while spikes and ticks take the pins at **4 cycles against 176, a 44x asymmetry that is a Tiny Tapeout tile's pin budget showing through**. `aer_out_ack` is tied low, because the serial read is itself the pop and two pop paths into one show-ahead register are two ways to lose an event. **The barrier is what makes the read loop legitimate**: it has no timing knowledge and reads until the SYNC it injected comes back carrying its own frame number. **Measured: 176 cycles per register access** (of which 171 is the frame, against a protocol floor of 168 the test asserts against), **185,443 → 213,971 on the whole program, +15.4 %** — of which about **13,840 is console output** — and, before the checks were written, **185,443 to the cycle with the block instantiated and untouched**, because the interrupt mask resets to zero and both engines reset disabled. **A fabric property becomes visible for the first time:** `soc_bus.v`'s arbiter picks a winner before it looks at the target's grant, so a slave withholding `gnt` blocks the OTHER master — F9's "every slave ready" antecedent, `docs/39` section 8 defect 3, now with a 176-cycle slave behind it. It costs nothing here **because Ibex stalls the pipeline on a load and never issues a second request**, which is reported as a measurement of this SoC and not as a property of the fabric. **The interrupt spends nothing that was spare**: `docs/40` assigned NPUCFG source 24 on fast line 12 before this block existed, lines 13 and 14 are still unassigned and a test derives that; one line covers sixteen nodes because a cause register and not a wire says what happened, **five of its seven bits come straight off the die's fault pins** — `docs/08` section 2.3's scope-visibility convention paying for itself somewhere it was not designed for — and the mix of five levels and two stickies follows `docs/50` section 5.1's rule that a flag must not be given semantics its source cannot support. **Verification: 21 cocotb tests with the frozen pilot IN the design under test** (every register value travels a real 40-bit frame), **14 mutations of which 10 are caught, one is caught only by the induction proof, one is a mutation that is not a violation, one guards a condition unreachable in this SoC, and one found a REAL GAP** — `gnt_o = req_i` passed all 21 tests until the grant test was rewritten to hold the request through the frame instead of dropping it on the first grant. **Formal: 4 tasks on the transport, all PASS, 5 of 5 covers reached, at two values of `SER_HALF` because H1's whole content is a rate** — and the property set adds **H4, a setup obligation `pilot_top.v` section 2 does not number**, which is why nobody was checking it. Three defects found and recorded, all in this work and all of the same shape: `soc_npu_ser` drops `busy_o` and raises `done_o` at the same edge, so **both FSMs that used one state to start a frame and watch for its completion launched a spurious second frame on the way out** — it duplicated a barrier in a twelve-event stream, and on the window it made a read immediately after a write to the same register return zero, but only when the two were close enough together that the spurious frame had not drained. **Measured area: the connection alone is 57,805.50 um2 (20.97 % of Ibex) with the pilot black-boxed, cross-checked to 1.4 % against subtracting `pilot_top`'s 152,717.18 from the subsystem's 209,739.08; the sixth fabric port is +56 cells, +6 flip-flops and 1,011.53 um2 — the six flops are one response-ownership queue and the arithmetic checks — measured against a control re-run at HEAD (812 / 9,917.36) rather than against `docs/40`'s published 808 / 9,688.29, because `soc_bus.v` changed in `docs/47` and `docs/50` section 2.2 is why a control is measured. More than half the connection is the two `aer_fifo` queues (53.5 %), and a cheaper queue was available and declined.** **Descriptor rings are not built and are priced rather than deferred**: a ring is a bus master, this fabric has two hardcoded master ports and a fairness property written for two, and at this scale the CPU is not the bottleneck — the transport is, and a ring does not touch it. Not hardened beyond what the queues inherit; no second node, no QSPI weight path, no place-and-route with a block that is 76 % of Ibex by cell area. **The next block is the fault-injection campaign through the connection**, because it sits between two measured things and is itself unmeasured, and hardening before measuring is the mistake `docs/38` section 10 item 4 names. |
| `docs/52-npu-connection-fault-injection.md` | `docs/51` section 14 item 7 records that nothing measures what an upset in the transport or the event engine does to an inference, and section 18 makes the campaign the next block because **hardening before measuring is the mistake `docs/38` section 10 item 4 names**. **This is that campaign: 700 injections, seven strata of 100, each run twice — 1,400 simulations, 31 m 34 s on 18 processes.** **The classification's SDC column is 0 of 700 and this document refuses to quote it**, because `fi_npu.c` checks the inference against `sw/golden/lif_core.py` and a nonzero fail mask is an announcement channel, so a corrupted inference is DETECTED by construction. **The number that replaces it is what an application would see: 38 of the 41 wrong inferences were silent to every hardware channel — 4.2 % ± 1.6 design-weighted over the connection's 738 injectable bits.** **The three populations are not equivalent and the ranking is neither by size nor by rate.** `evq_data` is **41.2 % of the flip-flops and contributes zero** — 98 of 100 MASKED, because the queues hold two live entries out of eight and most of those 304 bits are storage nothing reads — so a hardening wave aimed at the biggest structure would have spent its whole budget there. `cfgreg` is **thirteen flip-flops with the highest per-bit consequence in the campaign and contributes 0.07 points**, producing **a false fault report in 16 of 100 draws**: seven `sticky_inj_ovf`, seven `sticky_fetch_er`, and 28 of the 30 false alarms set nothing else at all, which is `docs/41` section 3.1's persistent-and-silent criterion producing exactly the failure it predicts. The rate lives in `ser` (2.18 points) and `engine` (1.71). **The block's only lethal failure is a lost fabric response and it is a handful of flip-flops: 7 dead machines in 600, four `ser.tick`, one `ser.state`, two `win_state`** — a corrupted phase leaves a frame that never ends, `busy_o` never falls, `rvalid` never returns and a two-stage in-order core stalls for ever. **The watchdog catches 7 of 7 [64.6–100], all at stage 2, all seven then completing with the golden answer, with zero spurious escalations in 607 survivable upsets** — the same paired counterfactual `docs/42` ran, asking a question nothing here had asked: whether the backstop reaches a CPU stalled on a peripheral. **A second counterfactual, on the connection's one recovery mechanism**, rebuilds `soc_npu.v` without the bounded fetch wait by an asserted substitution and replays the three records it fired on: **one goes from a correct inference plus a fault flag to no inference at all**, the other two lose only the report — a positive result where `docs/43` section 8.4's W7 was a zero, because a mechanism's value is a property of the pair. **The golden model bought nine records in which the spike stream was right and the neuron state file was wrong** — the architectural-state class `docs/42` section 9 names as the single largest reason its own rate is a lower bound, produced nine times here and MASKED under any output-only oracle — and it separated 41 wrong inferences from 45 deviations that were only a moved counter. **`docs/51` section 18's reversal condition is met**: 14 of 265 bring-up-phase draws corrupted the inference, the sequence's own read-backs caught 3, all 14 were silent, and the clearest is `ser.tx` bit 32 — `ADDR[0]`, so the write went to a register nobody checked, and the die's ECC encodes a wrong value as a **valid codeword**. **The exposure arithmetic makes 176 cycles a reliability statement: the transport is 18.2 % of the flip-flops and busy 84.9 % of the window, and every consequential upset in either half of it landed while a frame was in flight — all 27 idle-cycle deposits were MASKED**, because `ST_IDLE` reloads the whole frame state. **The same frame measured on both sides of the pin boundary**: `ser` 12 silent wrong inferences per 100 over 134 bits, the frozen `die_ser` 11 over 87, neither with any way to tell a host a frame went wrong — and all five dead machines on the SoC's side, because the die is a slave and returns wrong data rather than no data. **79 upsets were absorbed by mechanisms that worked and no software or pin could see one of them**: 74 pointer-vote corrections, all golden, all invisible, plus five rail disagreements. Controls: 96 sites and 825 bits agreeing field for field, no target a voted wire, the clean run silent in the NPU's own channels as well as the core's, the program's hang bound measured at 8.0x the clean run's worst, and a positive control that **failed and was right to** — it drew before the block was enabled, and the fix was to measure the enable cycle rather than move the constant. **Coverage 730 of 730 connection bits, 100 %**, by an independent Yosys census whose first version reported 64.9 % from a regex that folded eight queue slots into one and cancelled two errors into a plausible hole. Two harness defects recorded: verifying deposits after 1,400 simulations instead of inside the worker cost 37 minutes to an X-valued queue slot with no reset, and two campaigns were once launched against one output directory and both killed. Section 13 lists twelve things a clean number here would still not license, first among them that the zero SDC is a property of the oracle. **Nothing in `hw/rtl/` was modified and the flow refuses to elaborate if it is dirty.** |
| `docs/53-workload-and-the-clock.md` | `docs/48` section 7a, `docs/49` section 11.2 and `docs/50` section 7.4 all ended by saying the obligation was to **size the workload** before writing a frequency on a datasheet, and all three left it to another document. **This is that document, and the answer is that no mission profile in `docs/05` section 3.4 requires 50 MHz — and that 43.10 MHz must not be written down either.** The four profiles are turned into cycles per second with the derivation exposed rather than the conclusion asserted: the event-camera profile is sized against **Falcon Neuro**, the two DAVIS240C sensors flown on the ISS for exactly the lightning and sprite detection `docs/05` names, whose paper reports **16,700 events/s time-weighted, 53,444 events/s recording-weighted, sd 134,982** — and it costs **5.9 % of the cycle budget at 43.10 MHz against 5.1 % at 50, 10.2 % against 8.8 % at two standard deviations above the mean**. Nine OPS-SAT-AD telemetry channels cost **0.0044 %**. "Faster than a ground loop" is met by **76,551x**. Four assumptions carry the derivation, each numbered at the point of assuming, and the one the conclusion is most sensitive to — a 1 kHz frame rate — is swept over three decades: **the ceiling is 19,505 Hz at 43.10 MHz and 22,655 at 50**, so the assumption must be wrong by **20x** before the clock is in the argument. **What the design delivers was measured rather than quoted**, on `soc_npu` with the frozen pilot inside it, by a harness deliberately outside the working tree that imports the existing suite's drivers and bring-up: six workloads, from which **182.5714 cycles per serial frame and 6.8571 per inbound pin event** are SOLVED from two rows and then predict the other four to within **0.8 %**, with two independent cross-checks — one register access measured at **176 cycles** from cold reset, and the injection engine's own pin rate measured at **exactly 9.00 cycles per event** by counting strobes with no APB transfer in the loop. **The serial transport is 93 to 98 % of the cycles in every workload**, and making the workload seven times denser moves that by four points. **`docs/51`'s 44x asymmetry is 27x when both sides are measured the same way**, because 4 cycles is the pin contract and 6.86 is the delivered cost. **And the finding that reframes the question is a power number nobody had opened.** `docs/45` section 8, `docs/47` section 9, `docs/48` section 8, `docs/49` section 12 and `docs/50` section 10 all end with "no power number" — **`full3`'s own sign-off step wrote one, at three corners, on extracted parasitics: 27.774 / 34.835 / 46.095 mW**, in the run directory all five report from. It is `report_power` with no VCD, so it is an estimate wearing a measurement's clothes and is used only where an order of magnitude is what the requirement is stated to — and **the only number in `docs/05` section 3.4 is "tens of milliwatts"**. On it, **43.10 MHz is 13.8 % BETTER than 50**, and **energy per inference is invariant under the clock to 0.045 %** — 2.353 uJ against 2.354 uJ for the six-frame inference — because it is cycles times energy per cycle and the period appears in neither. **Four documents optimised the one term of the product the mission's stated requirement does not contain.** The recommendation is shape (c): **retire 50 MHz as a requirement, keep 20 ns as the SDC period** so that four rounds of slack stay comparable, **keep the setup failure as the defect it is**, and **change the RANK** — first a `report_power` with real switching activity, because profile 4 wants microwatts when idle and this part burns 27.8 to 46.1 mW at a 0.0044 % duty cycle with one clock and one mode, a **four-order** gap against the clock's 16 %; then the transport, where `docs/10` section 8 item 2's mesh link is a factor of **182** against the clock's **1.16**; and only then the timing. **43.10 MHz is refused explicitly**, on three grounds of which the first is that it is the number the layout reached, the second is that `docs/05` section 4 rule 5 forbids publishing a measured non-closure from a layout that fails setup, max slew and max cap and has never seen Magic DRC or LVS, and the third is that `tt/info.yaml` declares 50 MHz on a die being fabricated. The GR801 sanity check: the clock gap to the reference is **7.0x** and the memory gap **70–90x**, so the shortfall is in size and not in rate — and the one place the two sets of numbers meet is the outbound serial ceiling, **236,061/s at 43.10 against a 323,408/s burst**, which neither clock reaches and the mesh link does. Section 10 states at length what one simulated inference on one geometry does not establish. |
| `docs/54-klayout-deck-and-the-macro.md` | `docs/12` section 8.2 named two exits from the RM_IHPSG13 no-go and rejected the second in one clause — *"the KLayout deck also returns 2,316 errors, so it is not a free exit either"* — and never examined those 2,316. **This examines them, and the exit stays shut for a better reason than the number.** The count reproduces three ways: from the retained `macro-03` report, from a fresh run of the same deck eight days later, and — this is new — from the **bare vendor macro GDS checked entirely on its own**, which gives byte-identical 1,022 / 1,022 / 272 and sharpens `docs/12`'s "100 % inside the macro" into **100 % a property of the vendor cell as shipped, with this project's flow contributing zero**. **But 2,316 is not 2,316, and the caveat runs opposite to Magic's.** `Sdiod.d` and `Sdiod.e` flag the **identical 1,022 polygons** — intersection 1,022, symmetric difference 0 — so the headline double-counts; and the deep-mode figure is a *hierarchically deduplicated marker count* that the same deck in flat mode on the same file returns as **122,814**, a factor of **53.0**, unstable even under deck-assembly changes that leave the flat count fixed. Magic's raw count overstates by 3–4x, KLayout's understates by 53x, and **`docs/12`'s "nearly three orders of magnitude" between the decks is largely an artefact of comparing the two conventions**: normalised it is a factor of **4 to 9**, and on the contact-enclosure rule the decks **corroborate** rather than disagree. **`Sdiod.d`/`Sdiod.e` are an artefact and the case does not depend on which deck is authoritative**: they check a Schottky diode against nBuLay, the macro carries **no drawn nBuLay at all** — the deck's own log line is `nbulay_drw has 0 polygons` — the layer is **synthesized from NWell** by a shrink-grow, restricting the rule to the drawn layer returns **0**, device recognition is merely "any non-square contact" with no marker required, and the reported shapes are **boolean-clip remnants**: no drawn contact in the file has any dimension under 0.16 um and **2,303 of the 9,275 flagged shapes do**, down to 0.16 x 0.01 um. **`Cnt.c.digibnd` is the opposite and is not waived.** 99.93 % of its errors sit at exactly **0.02 um** Activ enclosure against 0.05 required — systematic, with flat and deep agreeing, so not a hierarchy or clipping artefact — and **the working hypothesis that `digibnd` is a marker a hard macro may not carry is wrong in direction**: DigiBnd is a *relaxation* from 0.07, the macro carries 21,171 of them, and deleting it would give **104,285** errors instead of 104,264. All of it lies inside a **vendor-drawn SRAM marker (25/0, 19,008 polygons, 66.9 % of the macro) that this deck reads, logs, and then uses in no rule whatsoever**, while `sg13g2_maximal.drc` in the same PDK commit exempts SRAM Activ and takes 104,264 to **0** — that deck returning **0 errors in 371 rules** on the unmodified macro and on the full `sram_pilot` GDS, with a positive control proving it can still report 5 errors on a deliberately malformed file. **The document then argues against its own most quotable result**: the deck returning 2,316 is the PDK's **main** set, *"thoroughly verified, tested ... intended for foundry precheck"*, and the one returning 0 is the **residual** set, *"not verified or tested"*, run by `run_drc.py` **in addition** to the main set, so a complete PDK run is still 2,316 and a `KLAYOUT_DRC_RUNSET` swap — a one-line config change — trades a verified deck for an unverified one. **Verdict: KLayout sign-off is not viable, and the deck swap moves nothing that matters**, because **Netgen LVS's 359 stand untouched** — the hierarchy-name half is IHP-Open-PDK #239 per `docs/04` section 1.2 — so even with both scoping defects fixed upstream the picture is "one deck passes, one deck fails, LVS fails", which is a better-understood failure and not a passing one. **`docs/12`'s NO-GO stands; two of its sentences are amended** and two reproducible upstream reports are drafted, the load-bearing one being a question this document refuses to answer for itself: **is 0.02 um enclosure inside the SRAM marker a qualified rule?** A vendor drawing a marker is not a foundry qualifying the geometry under it. **The pilot cannot be affected and the reason is structural, not a judgement**: `sg13g2_stdcell` has **zero non-square contacts and zero DigiBnd**, so both rules are inert on any standard-cell-only design — measured on `aer_fifo`, which carries **7.5x more synthesized nBuLay area than the macro** and still scores 0, proving the discriminator is the contact and not the well. `docs/31`'s zeros stand; no run directory was created in the repository and nothing outside this file and its index row was modified. |
| `docs/55-npu-connection-hardening.md` | `docs/52` measured the NPU connection and ranked what to protect **by consequence rather than by size**; this builds that ranking, in that order, and re-runs the campaign against what it built. The connection's **only lethal failure** was a lost fabric response — a frame that never ends, so `rvalid` never returns and Ibex stalls for ever — and the answer was a watchdog system reset. Two bounded waits, the pattern `hw/rtl/pilot_top.v` and `soc_npu.v` already apply twice, replace it with a **load access fault the program can handle** and a named cause bit. `hw/soc/formal/soc_npu_ser_props.v` I10 states the transport's guard as an exact function of the frame position and I11 concludes it **cannot fire on a healthy frame**; the window's bound rests on an argument about the arbiter and is therefore measured twice instead. The queues' protection, which absorbed **79 upsets no operator could see**, now raises a sticky bit in the block's own cause register and a saturating counter in `BUSSTAT` — answering `docs/51` section 14 item 1's objection, which was about where counters live, rather than overruling it. The control and cause bank is **tripled**: nine of its eleven fields are one bit wide and cannot be replicated alone, so they are bundled into one 21-bit word under `soc_tmr_bank` and `hw/rtl/tmr_voter.v`, and the voter's own report is a **field of the protected word**. **`evq_data` is deliberately left alone** — 41.2 % of the flip-flops, 53.5 % of the area, and zero silent wrong inferences in 100 draws. The campaign is re-run twice and the headline is a verbatim replay of `docs/52`'s own draws: **7 of 7 dead machines become 0**, and a fresh 700-injection campaign finds **none in 700 with the watchdog held off for the whole run** — the backstop is no longer load-bearing for this block. The queues' 79 invisible absorbed upsets become 75 announced in `IRQ_CAUSE` and every one counted in `BUSSTAT`, with the operator's count equal to the bench's rather than close to it, and the cause register's **16 fabricated faults in 100 become 0**. What did NOT move is the silent-corruption rate, and the document says so with the control that proves it: the FROZEN die's own stratum, silicon that cannot have changed, moved by one with fifteen records changing verdict, so the apparent improvement is workload drift. **The window's bound was built wrong first and the re-run is what caught it** — armed by the FSM state it missed two of the seven, because the failure is not a window that waits for ever but one that forgets it was waiting, and one that is busy with nothing owed; both are facts about the fabric handshake and are answered from the grant. Cost, against the `HARDEN = 0` baseline `docs/41` section 6.5 insists on: **+42 flip-flops and 4,761.2502 um2** for the redundancy, **12,696.4530 um2** for the whole wave including `BUSSTAT`'s three counters, 4.61 % of the unprotected core. The netlist census says the three banks survive with every attribute deleted from the text — and it caught one predicted number being wrong, because a **clearable** sticky bit is one `opt_dff` cannot fold away and an unclearable one is. |
| `docs/56-npu-event-engine-hardening.md` | `docs/55` section 14 named the event engine as the next block and attached a condition that was not optional: **split its 140-bit stratum first**, because an undifferentiated stratum is hardened by protecting its largest structure — which is the instinct `docs/52` already measured being wrong, when `evq_data` turned out to be 41.2 % of the flip-flops and zero of the rate. Split into five and drawn 100 times each, **the engine's rate is in its STROBES and not in its data**: `ev_data` at 64 bits — 45.7 % of the engine, the largest sub-stratum by a factor of three — came back **0 of 100**, `ev_cnt`'s 32 bits came back 0 of 100, and **96 % of the whole stratum's silent corruption sits in NINE of its 140 flip-flops**, every one of them a one-bit control flag or the four-bit state that drives them. The split carries its own control and it is exact: the six strata it did not touch came back **identical, row for row and record for record**, to `docs/55`'s campaign of record. **The worst site in any campaign this block has had is `aer_in_stb` at 18 silent wrong inferences in 18 draws** — one flip-flop, whose upset strobed a phantom SPIKE into the frozen die with whatever the address pins held, and the die accepted it. **The answer costs no flip-flop**: `aer_in_stb == (ev_state == E_PIN_S)` was already an invariant of the FSM, so the redundancy was in the netlist and was not being read; the pin is now the AND of the two and a disagreement raises a cause bit. Measured by mutation at **+22 cells, 159.0246 um2 and the identical flip-flop count**. The second item bounds the show-ahead adapter's read of the capture queue, and it found something **2,500 injections across three campaigns had not**: `oh_req` was cleared by `cap_rd_valid` alone while the refill stood off on `!oh_req`, so **one discarded capture entry — `aer_fifo`'s own documented behaviour, no upset needed in the adapter — stopped the block delivering events for ever** while `IRQ_CAUSE.EVT` went on saying one was waiting. Both ways in are measured in simulation and both failed against the design `docs/55` shipped. **TMR could not have found it**, because a wedged `oh_req` is a value all three replicas agree on. Result, on 1,000 verbatim replayed injections: **51 silent wrong inferences become 30, and every one of the 21 is in a stratum this work touched** — `ev_pin` **18 to 0**, with 17 of the 18 draws into `aer_in_stb` now returning **the golden inference** and a named cause bit, and `oh_req` 3 to 0. **The calibration is exact rather than argued**: this wave changes no software, so the clean run, the injection window and the 215,428-cycle invariant are all unmoved, and **exactly one record in a thousand changed its verdict** — the frozen die's 100, 400 more in untouched strata and 200 more inside the split stratum all come back record for record, where `docs/55` had to refuse a 19 % claim because its frozen die moved on 15 of 100. The one that moved is an improvement that reads like a regression and is spelled out: a run that was MASKED is now DETECTED because the part learned to speak. Cost **+15 flip-flops and 1,471.3650 um2**, 0.53 % of the unprotected core, of which **twelve are the two reports** and not the two mechanisms. What it did NOT do is stated: `ev_state` at 7 of 20 is unmoved, the gate fires on it 5 times and helps it 0 times — a prediction the campaign refuted — `oh_valid` and `cap_wr_en` are ranked and priced and not built, `soc_npu.v` still has no formal property set, and the two new reports have no durable record because a fourth `BUSSTAT` counter would be 18 more flip-flops in the block `docs/55` already ranked next. |
| `docs/57-power-under-a-duty-cycle.md` | `docs/53` section 15 item 1 ranked one measurement above everything else in the record — **`report_power` with the whole-SoC run's own switching activity** — and called it *"the smallest piece of work in the record that closes the largest requirement gap in it"*. **This is that measurement, and it finds two things nobody was looking for.** The first: **this design has a clock gate.** One `sg13g2_lgcp_1`, `u_ibex.core_clock_gate_i.u_icg`, sits in the signed-off netlist over **2,464 of its 3,085 flip-flops**, and `u_ibex.clk` toggles **1.9995 times per cycle awake and 0.0004 in WFI** **[fact]** — against six documents that say "one clock, one mode". The second: **the layout the power number comes from does not contain the NPU**, because `syn_soc_top.sh`'s source list still has no `soc_npu.v` in it **[fact]**; `docs/51` section 11 said so and no document after it carried the caveat. **Measured, at typ, on `docs/46`'s duty-cycled supervisor and its one-`wfi`-different twin: computing 33.890 mW, waiting 5.539 mW, the whole run 17.876 mW against the free-running build's 33.724** **[fact]** — so `docs/47`'s unannotated 34.835 mW was **right to 2.8 % for a busy part and 6.29x high for an idle one**, and `docs/53` section 7.3's "this design idles at 27.8 to 46.1 mW" is wrong by that factor. Three independent checks: the duty cycle **composes to 1.8 %**, the two dispatch disciplines agree on the work to **0.5 %**, and a 120-cycle idle window in a different program on a different testbench gives **5.619 mW against 5.539** **[fact]**. Where it goes, at typ: sequential / combinational / clock / macro is **10.749 / 13.880 / 3.828 / 5.433 mW** computing and **2.414 / 1.672 / 1.281 / 0.173** waiting, and idle switching is **three free-running counters in 231 of 2,905 flip-flops** — `u_clint` 52.4 %, `u_timer0` 24.6 %, `u_uart0` 23.0 %, `u_ibex` **0.03 %** **[fact]**. The six macros are priced twice, by `report_power` and independently from the Liberty's own `when` tables against measured access counts, and the second instrument finds **0.147 mW of idle power — 20.7x the macros' whole leakage — burnt because `A_REN` is tied to `!do_write` and is therefore high on every deselected macro on every clock edge** **[fact]**; the fix is six AND gates. The mission answer changes on both sides: orbit-average at profile 1's own 0.0044 % duty is **5.540 mW**, single-digit milliwatts against a requirement stated in tens, and profile 4's microwatt gap is **1.7 to 2.4 orders and not four**. Energy per inference is re-measured through the whole SoC at **3,594 cycles and 2.400 uJ** — 2.0 % from `docs/53`'s 2.353 because **both of that figure's terms were wrong in opposite directions** — and its invariance under the clock is reproduced at **+0.047 %**. Section 6 is why the recommendation is about clocks and not about activity: with the clock running the floor is **30.5 mW** and the entire measured activity range is worth **1.2 mW**, against the existing gate's **28.4**. Two defects found on the way: **`fi_core.sh` has not elaborated since `docs/51`**, so `docs/42`, `docs/43` and `docs/46`'s campaigns cannot be rebuilt, and the same gap in `syn_soc_top.sh` is why the layout is missing **50.7 % of the design's cells and 69.8 % of its flip-flops** — whose power ratio at the same activity is **1.465x busy and 5.588x idle**, because the accelerator has no gate. **Section 16: harden the SoC that exists, before another document ranks anything against numbers from the one that does not.** |
| `docs/58-clint-time-base-hardening.md` | `mtime` had been ranked above everything left unprotected by `docs/41` section 7.4, second by `docs/43` section 12 and first by `docs/44` section 3, and deferred three times: an upset in it is **persistent and silent** — it displaces the architectural clock for ever and every deadline software computes as `mtime + delta` is then wrong — but it cannot disarm anything, so the backstop went first. This protects it, and **not by replication**. A monotone counter is checkable without a second copy of itself, so `mtime` becomes the data field of a (72,64) SECDED codeword decoded, corrected and **re-encoded on every tick**: the scrub period is one cycle, there is no scrubber to build and no accumulation to bound, on **eight added flip-flops against TMR's 128**. `hw/rtl/secded_enc.v` and `secded_dec.v` are read in place — they are fixed at 64 data bits by an elaboration guard and 64 is exactly `mtime`'s width — and nothing in the frozen `hw/rtl` is modified. **The document's own negative result is the answer `docs/41` suggested**: a residue check was priced there in flip-flops, and measured in silicon a **residue-mod-3 detector costs 98.694 % of what the corrector costs and corrects nothing**, because the cost of any check over a 64-bit counter is the reduction tree and not the shadow register. Parity, the genuinely cheap detector, is 36.0 %. Six designs were synthesised against one baseline with one source list, and **`docs/41`'s unsynthesised +27,000 um2 estimate for TMR is measured at +25,792.0740 um2 and exactly +256 flip-flops** — 4.68 % high, and now a fact. The alternative nobody had raised is checked and inverted: **11 of the 64 bits are architecturally dead over a five-year mission**, and that is an argument FOR covering them, because deadness bounds the fault-free value and not the faulty one — bit 63 is a 5,845-year displacement — while truncating to 54 bits is measured at −1,438.1766 um2 and leaves bit 53 at 5.71 years, which still ends the mission. `mtimecmp` gets **nothing**, argued and then measured: over 128 injections it displaced the clock **zero** times, moved the deadline 117 times and removed it 60 times, and a removed deadline is precisely what the watchdog is the backstop for. Result on 342 injections: **128 of 128 draws into `mtime` and 16 of 16 into the check bits come back CORRECTED, clock unchanged, deadline on its golden cycle, every one announced**, where the same draws against the unprotected design displaced the clock 95 times with a worst case of **2^63 ticks — 5,845 years — and announced none of them**. The calibration is exact: this wave changes no software, so **not one of the 198 records in the strata it does not touch changed anything**, and the whole-SoC invariant is unmoved at **215,428**. Cost **+8,944.2360 um2 and +26 flip-flops**, 3.244 % of the unprotected core, of which **eighteen are the report** — the eighth and last BUSSTAT source, which takes the one bit left below an interrupt bit that may not move and therefore **conflates a correction with an uncorrectable**, a cost named rather than glossed. What it does not do is stated: a single-event transient in the codec's 528 combinational cells is a fault model no harness here can express and is a larger gap for a code than for a voted bank, **no STA has been run on this block before or after**, the recovery from an uncorrectable — re-epoch, and reconstruct absolute time from `mcycle`, which advances at `mtime`'s rate at `TICK_DIV = 1` — is [planned] with three caveats and no program behind it, and the proof got **183 times more expensive**. |
| `docs/59-floorplan-region-map.md` | The corpus states the die as tables in `docs/47` and `docs/48`; this is the picture, and the picture is generated. **Every region, coordinate and area on it is parsed out of `hw/soc/pnr/runs/full3/final/def/soc_top.def` by `hw/soc/pnr/floorplan_map.py`** — die box, core box, the union of 1,403 `ROW` statements, all six macro rectangles, 169,747 cell positions, both power nets and all 18 die ports — because a floorplan diagram is the one artefact here that a reader recognises instead of checking. The script proves itself two ways every run: **eight geometry self-checks** (no macro overlaps another, no cell overlaps a macro, every cell on a `ROW` y) and **ten cross-checks against the run's own `metrics.json`**, all eighteen passing, with the two areas agreeing to OpenROAD's rounding at **531,370.6272** and **2,246,899.8528 um2**. The macro total reproduces `docs/47` section 4.2 to the last digit by an independent path. **What the map makes visible that no table did**: 97.06 % of the standard cells and 3,021 of the 3,085 flip-flops are in the 700.08 um channel, **thirteen cells and no flip-flops sit above it**, and the region the placer actually used is **34.84 % dense, not the 18.9 % headline** — which is an average over 444,306.6432 um2 of row holding 1,107 cells. Three numbers move: **placeable area is 1,931,789.8656 um2, not the 2,092,010.9472 every document states**, because the 10 um macro halos and the site grid take 7.66 % the arithmetic did not; `docs/47`'s **523,809.64 um2 of standard cells is the rejected `deferred_flatten` netlist**, so the memory/logic ratio is **4.2285** against what was placed, not 4.29; and `docs/48`'s CTS span **reproduces at origin (388.8, 582.12) exactly** while growing one site and one row to 1,857.6 × 793.8 by sign-off. Transistors are counted, not guessed — 28 PDK CDL files flattened to **4,223,084 devices, 87.5 % of them SRAM bitcells** — which puts GR801's 11.2 MB at **74.8 times this entire die** in these macros. **Two things are refused.** There are no logic blocks on the map because there are none in the layout: the run hardened with `SYNTH_HIERARCHY_MODE=flatten` and `SYNTH_AUTONAME=0`, so **751 of 169,747 component names carry an RTL path and 750 of those are memory tie cells**; drawing an Ibex box would be fiction, and section 6.3 states what would be needed instead. And **`soc_npu` is not on this die at all** — the run's own `VERILOG_FILES` names 50 sources and not one is the NPU, which `docs/51` integrated afterwards — so the neuromorphic accelerator this SoC exists for has never been placed. The four failing checks, the 2,332.02 um net longer than the die is wide, and the fact that **nothing here has ever been through Magic DRC, LVS or XOR** are printed on the SVG itself, because the picture will travel without the document. |
| `docs/60-soc-datasheet.md` | The SoC's datasheet, which `docs/21` is not: that one describes the frozen shuttle tile, and the part it becomes a node of has existed only as scattered sections across twenty documents since. Preliminary and pre-silicon, and the framing is on the first page rather than in an appendix, because **the part described does not exist, is not manufacturable on this PDK version, and does not meet its own timing constraint** — there is no DRC, LVS or XOR result, the layout misses setup by **−3.2029 ns on 2,003 endpoints** and also fails hold, max cap and max slew, and the layout it reports **does not contain the NPU subsystem** at all. **No frequency figure is published**, on `docs/53` section 9.2's three grounds and `docs/05` section 4 rule 5; the power figures are `report_power` without activity annotation and are tagged **[in flux]**. What it does carry is the memory map and the interrupt assignment cited from their generated sources rather than retyped, a block-by-block functional description of Ibex, the fabric, the memories, the CLINT, the GPTIMER, the three-stage watchdog, BUSSTAT and the NPU node, **a "what this device does not do" section in the body** — no SpaceWire, CAN, SPI, I2C, QSPI or GPIO, eleven of sixteen peripheral slots empty, one node of sixteen, one clock and one mode — and a fault-tolerance section that is this design's actual distinguishing feature and is **measured**: seven RTL campaigns over five named populations, with every denominator stated and three documents' own refusals to quote an improvement carried forward rather than netted off. Section 14 records seven disagreements found while collecting the numbers, including that `hw/tb/fi_campaign_results.json` holds **378** injections where `docs/16` says 366 and `docs/21` still says 335. |
| `docs/61-soc-with-the-accelerator.md` | `docs/57` section 16 ranked one thing above everything else in the record — *"harden the SoC that exists"* — because `docs/45`, `docs/47`, `docs/48`, `docs/49`, `docs/50` and `docs/53` are six documents of area, timing and power for a `soc_top` **missing 50.7 % of its cells**: `docs/51` put `u_npu` inside it and updated ONE of the four flow scripts that build the file. **This is that run**, and its first result is that the accelerator **fits in `docs/47`'s floorplan with no die growth at all**: the same 2306.40 x 2075.22 um die, the same 700.08 um channel, the same six macro origins, holding **624,429.9180 um2 of standard cells against 396,177.6420** — and `PL_TARGET_DENSITY_PCT` 40 -> 48 by `floorplan.py`'s own rule is **the only key in the configuration that moves**. The design is **42,518 cells, 5,263 flip-flops** against 27,565 and 3,085 **[fact]**, whose **+26 flip-flops over `docs/57`'s prediction are exactly `docs/58`'s published cost** for the `mtime` codeword. **At sign-off it misses 20 ns by 3.5789 ns on 2,175 endpoints against `docs/47`'s 3.2029 on 2,003 — a delta of 0.3760 ns, inside `docs/44`'s measured 0.4 ns of instrument noise, and therefore NOT claimed in either direction**; TNS is 43.7 % worse, which is outside it and is; **hold closes at all three corners with no key changed**, where `docs/47` failed on 3 fast-corner endpoints and `docs/48` had to spend 1.65 ns of setup to fix it; detailed routing reaches **0 DRC** from 32,412 violations, and the **longest net gets shorter**, 2,332.02 -> 2,315.01 um. Max slew and max cap get worse, 14/0/1 -> 29/8/6 and 10/10/12 -> 15/15/15. **Eight instruments reproduce first, four byte-exactly**, including `docs/49` section 3.1's 444/1,559 violator split from a script that is now in the tree and `docs/42`'s clean fault-injection run at **18,682 cycles, sig `9c07ef12`**, from the repaired `fi_core.sh`. **The four measured remedies are re-asked and only one survives unchanged.** `docs/49`'s disjoint-cone result is **reversed on the condition `docs/49` itself wrote**: **2,133 of 2,175 violating endpoints now launch from two bits of one register-file read address**, the worst path runs `raddr_b_i[3]` -> `rf_chk_q[2]` through the 32-way read multiplexer `SYNPRE` restructures, and **only 9 launch from a memory macro against 444**, so `docs/50`'s read register is **moot for the worst slack** and its 25 % cycle price is now unmeasured. `docs/48`'s floorplan result stands and both of its mechanisms fail: the placer **is** density-constrained now (the routability optimiser stops on its iteration cap at 1.0112 against a target of 1.0100), and its "cells / their own bounding box" predictor falls 27.1 % -> 19.3 % **with no timing cost**, which measures that correlation wrong. **Clock-tree synthesis counts the accelerator's flip-flops independently and confirms `docs/57` section 9's mechanism**: `clk_i_regs` goes **762 -> 2,940 sinks while `u_ibex.clk` stays at 2,323**, so all 2,178 new flip-flops are ungated and the ungated share of the design goes 24.7 % -> 55.9 % — the power ratio behind it stays an estimate, because no power was measured here. The die is now **50.44 % dense in the channel against `docs/59`'s 34.84 %** with **2,485 cells above it where thirteen sat**, and the memory/logic ratio falls 4.2285 -> 2.7445. **`fi_core.sh` is repaired** and `docs/42`'s campaign can be rebuilt — though **no campaign is re-run**, which is said in the body rather than left to be assumed — and a mutation-tested guard now fails whenever a flow stops naming a module `soc_top.v` instantiates, which it would have done **nine times** at the previous commit. **17 of 19 checkers gate, the same two do not, and `Checker.HoldViolations` examined three corners before reporting all zero.** Still no DRC, no LVS, no XOR, no gate-level simulation, no power number and **no frequency**. |
| `docs/62-synpre-remeasured.md` | `docs/49` declined `SYNPRE` because it restructures the register file's read-side decoder while the binding path ran from a RAM macro through the load return into the WRITE encoder — disjoint cones — and it wrote down the condition under which that conclusion would expire. `docs/61` met the condition: **2,133 of 2,175 violating endpoints now launch from two register-file read-address bits and only nine from a macro**. **This is the re-measurement, carried to extracted parasitics and three-corner sign-off on `docs/61`'s floorplan, and `SYNPRE` is worse than it was when it was on the wrong cone.** At sign-off: **−4.5898 ns on 2,607 endpoints against −3.5789 on 2,175 — a delta of −1.0109 ns, which is 2.5 times `docs/44`'s measured 0.4 ns noise floor and IS therefore reported**, with TNS 62.5 % worse at −4,844.60. **`docs/49`'s +3.4641 ns at post-CTS did not get given back by routing; it never appeared** — the same step moves −0.2014 ns, and the mechanism is visible as cells: `docs/49`'s post-CTS worst path went from five `xnor2` to two, this one goes from **three to eleven**. **The structural reason reconciles the two earlier documents and both were right.** The binding path launches at a read address and captures at a register-file SECDED **check bit**, whose D input is `u_enc_wr.check_out` — so it runs through `SYNPRE`'s cone AND through the write encoder `docs/49` proved untouchable, and the second half carries most of the XOR. **`SYNPRE` made the binding path longer by every measure**: 79 cells → **113**, 14 `xnor2` → **17**, arrival 24.9617 → **26.0195 ns**. **The cost is recomputed and not scaled**: **+30,850.4322 um2 and +1,589 cells, +4.9406 %** of this design's standard cells — **`docs/61`'s +4.4 % estimate is corrected**, because it carried `docs/49`'s absolute figure onto a new denominator, and the same change costs more area and FEWER cells here (`docs/45` 4.3 again). **`docs/61`'s two warnings both land.** The placer's final weighted routing congestion goes from **0.9960, below target, to 1.0424, above it**, with routability inflation **+1.79 % → +9.67 %, a factor of 5.57**, on HPWL up **25.07 %**; and max slew **29/8/6 → 83/3/1** with max cap **15/15/15 → 34/34/36** against the library's own limit in both runs. **Where the path lands**: not back at the macros — that launch group goes **9 → 3** — and the read-address group is **not emptied but split**, 1,514 endpoints moving to a stored-data launch (`SYNPRE`'s signature) while **1,050 stay on read addresses and the design's worst slack is on one of them**. **Thirteen instruments reproduce first**, including `docs/61`'s whole sign-off block, its violator census, its CTS span re-derived independently from the DEF, and its entire cell census. **Verification re-run, not inherited**: C5/C6/C7 pass on four engines with **9 of 9 covers**, the 124-injection campaign is identical field for field, and the whole-SoC invariant is **cycle-identical to HEAD's baseline** — measured beside it in the same session, because `docs/49`'s invariant predates the accelerator. **17 of 19 checkers gate, the same two do not**, and the audit's job here is the inverse of usual: it establishes that a WORSE number is a worse number and not a checker that started looking harder. Costs **230.0 min against 135.9, 1.69x**, the post-CTS resizer alone **2 h 04 against 22 min**. `route__wirelength__max` is **2,789.72 um on a die 2,306.40 um wide**, which retires `docs/48`'s reading of that metric. Still no DRC, no LVS, no XOR, no gate-level simulation, no power number and **no frequency**. **`SYNPRE` is closed**: three documents have priced the same parameter and the price rose each time. |
| `docs/regmap-npu.md` | Generated register-map documentation. Single source: `regmap/regmap.yaml`. |
| `docs/memmap-soc.md` | Generated SoC memory map. Single source: `regmap/memmap.yaml`. |
| `README.md` | Project summary and repository layout. |
| `ROADMAP.md` | Phased plan with gates and external clocks. |

`sw/tests/test_doc_links.py` checks that this table names every document
in the corpus and that every cross-reference in every document names a
file that exists. A document added without an entry here fails that
test.

## 6. Counts that other documents state differently

`docs/11-verification-harness.md` opens with headline totals measured on
2026-08-25: 28 SymbiYosys tasks across five formal jobs, 109 distinct
cocotb tests across six suites, and 145 Python tests. All three were
correct on that date and all three have since been overtaken by the
`scrub` block and by wave-3 work. Re-measured on 2026-08-26 the same
quantities are 37 SymbiYosys tasks across six property sets, 129 cocotb
test functions across eight modules, and 153 collected Python tests of
which 152 pass and one skips — 185 collected once this index's own link
check is counted. **[fact]** Nothing about the verification
argument changes; only the arithmetic does. This section is a pointer,
not a correction — `docs/11` remains the authority on what each suite
means.

**Re-measured again 2026-08-29 [fact].** The 2026-08-26 formal figure
above has itself been overtaken, by the `lif_mem` property set and by
the tasks added with it. `make -C formal -n everything | grep -c
"sby -f"` now returns **45**, and the `[tasks]` sections of
`formal/*.sby` are **7** property sets, not six: `aer_fifo` 4,
`lif_ctrl` 8, `lif_mem` 8, `npu_regbank` 5, `scrub` 9, `secded` 3,
`tmr_voter` 8. The other two quantities in that line moved as well:
`grep -c '@cocotb.test' hw/tb/test_*.py` now totals **145** cocotb test
functions across the same eight modules (was 129), and
`.venv/bin/python -m pytest --collect-only -q` collects **295** Python
tests as of 2026-09-01 (196 on 2026-08-29, 185 at commit `fe89f0d`), and
running them gives **295 passed**. Section 2.1's rows carry
the new numbers; the 2026-08-26 and 2026-08-29 lines above are left as
written because each was correct on its date. **Collection is not
execution** — that distinction is why both are now given for the Python
suite, and the cocotb and formal counts on this page are still
collections only.

## 7. Reading this corpus as a site

`scripts/build_docs.py` renders every document in the table above into a
browsable, self-contained static site, resolving each bare `docs/NN`
cross-reference into a hyperlink and generating a table of contents per
document:

```
python3 scripts/build_docs.py --out _site
```

It uses pandoc when pandoc is on PATH and a built-in renderer otherwise,
and prints which one it used. `_site/` is build output and is not
tracked. `.github/workflows/docs.yml` builds the same site on every push
and uploads it as an artifact; it does not publish it, because
publication depends on `docs/14-licensing-decision.md`, which is
unsigned.
